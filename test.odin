package game

import "core:fmt"

/*main :: proc()
{
    arr0ptr, arr1ptr := return_array_addresses()

    append(arr0ptr, 0.1)
    append(arr1ptr, 0.1)
}

return_array_addresses :: proc() -> (^[dynamic]f32, ^[dynamic]f32)
{
    arr0 : [dynamic]f32
    arr1 := make([dynamic]f32)
    arr2 := new([dynamic]f32)

    return &arr0, arr2
}*/